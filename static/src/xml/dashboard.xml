<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="lms.Dashboard" owl="1">
        <div class="lms-dashboard-container" style="display:flex; height:100vh; font-family:Inter, sans-serif;">

            <!-- Sidebar -->
            <aside class="lms-sidebar" style="width:260px; background:#1e293b; color:#fff; padding:1rem; overflow-y:auto;">
                <div class="lms-logo" style="font-weight:600; font-size:1.1rem; margin-bottom:1rem;">🚚 Logistics Management</div>

                <ul class="lms-menu" style="list-style:none; padding:0; margin:0;">
                    <!-- Lead -->
                    <li t-on-click="() => setActiveSection('lead')" t-att-class="{'active': state.tab === 'lead'}" style="padding:.6rem 0; cursor:pointer;">
                        Lead Management
                    </li>

                    <!-- Warehouse -->
                    <li t-on-click="() => setActiveSection('warehouse')" t-att-class="{'active': state.tab === 'warehouse'}" style="padding:.6rem 0; cursor:pointer;">
                        Warehouse Management
                    </li>

                    <!-- Dispatch Management -->
                    <li>
                        <div t-on-click="() => toggleSubMenu('dispatch')" style="padding:.6rem 0; cursor:pointer; display:flex; justify-content:space-between; align-items:center;">
                            <span>Dispatch Management</span>
                            <span t-esc="state.expanded.dispatch ? '▾' : '▸'"/>
                        </div>
                        <ul t-if="state.expanded.dispatch" style="list-style:none; padding-left:1rem; margin:0;">
                            <li t-on-click="() => setActiveSection('trip_sheet')" style="padding:.3rem 0; cursor:pointer;">Trip Sheet</li>
                            <li t-on-click="() => setActiveSection('route_dispatch')" style="padding:.3rem 0; cursor:pointer;">Route Dispatch</li>
                            <li t-on-click="() => setActiveSection('lr')" style="padding:.3rem 0; cursor:pointer;">Lorry Receipt (LR)</li>
                            <li t-on-click="() => setActiveSection('pod')" style="padding:.3rem 0; cursor:pointer;">Proof of Delivery (POD)</li>
                            <li t-on-click="() => setActiveSection('ewaybill')" style="padding:.3rem 0; cursor:pointer;">E-Way Bill</li>
                            <li t-on-click="() => setActiveSection('dispatch_reports')" style="padding:.3rem 0; cursor:pointer;">Dispatch Reports</li>
                        </ul>
                    </li>

                    <!-- Logistics Operations -->
                    <li>
                        <div t-on-click="() => toggleSubMenu('operations')" style="padding:.6rem 0; cursor:pointer; display:flex; justify-content:space-between; align-items:center;">
                            <span>Logistics Operations Management</span>
                            <span t-esc="state.expanded.operations ? '▾' : '▸'"/>
                        </div>
                        <ul t-if="state.expanded.operations" style="list-style:none; padding-left:1rem; margin:0;">
                            <li t-on-click="() => setActiveSection('fleet_overview')" style="padding:.3rem 0; cursor:pointer;">Fleet Overview</li>
                            <li t-on-click="() => setActiveSection('vehicle_costs')" style="padding:.3rem 0; cursor:pointer;">Vehicle Costs</li>
                            <li t-on-click="() => setActiveSection('odoometer')" style="padding:.3rem 0; cursor:pointer;">Odometer</li>
                            <li t-on-click="() => setActiveSection('route_optimization')" style="padding:.3rem 0; cursor:pointer;">Route Optimization</li>
                            <li t-on-click="() => setActiveSection('fleet_reports')" style="padding:.3rem 0; cursor:pointer;">Fleet Reports</li>
                        </ul>
                    </li>
                    <!-- <li>
                        <div t-on-click="toggleSubMenu('finance')" style="padding:.5rem 0; cursor:pointer; display:flex; justify-content:space-between; align-items:center;">
                            <span>Finance and Accounting</span>
                            <span t-esc="state.expanded.finance ? '▾' : '▸'"/>
                        </div>
                        <ul t-if="state.expanded.finance" style="list-style:none; padding-left:1rem; margin:0;">
                            <li t-on-click="() => setActiveSection('finance_overview')" style="padding:.3rem 0; cursor:pointer;">Overview</li>
                            <li t-on-click="() => setActiveSection('customer_invoices')" style="padding:.3rem 0; cursor:pointer;">Customer Invoices</li>
                            <li t-on-click="() => setActiveSection('customer_credit_notes')" style="padding:.3rem 0; cursor:pointer;">Customer Credit Notes</li>
                            <li t-on-click="() => setActiveSection('customer_payments')" style="padding:.3rem 0; cursor:pointer;">Customer Payments</li>
                            <li t-on-click="() => setActiveSection('accounting_journals')" style="padding:.3rem 0; cursor:pointer;">Accounting Journals</li>
                            <li t-on-click="() => setActiveSection('accounting_journals_entries')" style="padding:.3rem 0; cursor:pointer;">Accounting Journals Entries</li>




                        </ul>
                    </li> -->
                    <li t-on-click="() => setActiveSection('rfq')" t-att-class="{'active': state.tab === 'rfq'}" style="padding:.6rem 0; cursor:pointer;">
                        RFP/RFQ
                    </li>
                </ul>
            </aside>

            <!-- Main Content -->
            <main class="lms-main" style="flex:1; background:#f8fafc; padding:1rem;">
                <t t-if="state.iframeSrc">
                    <div class="iframe-container" style="height:calc(100vh - 100px);">
                        <iframe t-att-src="state.iframeSrc" style="width:100%; height:100%; border:0; border-radius:6px;" frameborder="0"/>
                    </div>
                </t>
                <t t-if="!state.iframeSrc">
                    <div style="display:flex; align-items:center; justify-content:center; height:100%;">
                        <p style="font-size:1.1rem; color:#64748b;">Welcome 👋 — Select a module from the sidebar to begin.</p>
                    </div>
                </t>
            </main>
        </div>
    </t>
</templates>
